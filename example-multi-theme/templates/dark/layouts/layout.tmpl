<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    {{ template "header" . }}
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #e0e0e0; background: #121212; }
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        header { background: linear-gradient(135deg, #1a1a1a, #2d2d2d); color: #ffffff; padding: 1rem 0; box-shadow: 0 2px 8px rgba(0,0,0,0.3); }
        header h1 { font-size: 1.8rem; margin-bottom: 0.5rem; color: #bb86fc; }
        header .theme-info { font-size: 0.9rem; opacity: 0.8; margin-bottom: 0.5rem; color: #a0a0a0; }
        header nav { margin-top: 0.5rem; }
        header nav a { color: #e0e0e0; text-decoration: none; margin-right: 1.5rem; padding: 0.5rem 1rem; border-radius: 4px; transition: all 0.3s; border: 1px solid transparent; }
        header nav a:hover { background: rgba(187, 134, 252, 0.2); border-color: #bb86fc; }
        main { padding: 2rem 0; min-height: calc(100vh - 200px); }
        footer { background: #1a1a1a; color: #a0a0a0; padding: 1.5rem 0; text-align: center; border-top: 1px solid #333; }
        .theme-badge { display: inline-block; background: #bb86fc; color: #121212; padding: 0.2rem 0.5rem; border-radius: 3px; font-size: 0.8rem; margin-left: 0.5rem; font-weight: bold; }
    </style>
</head>

<body>
    <header>
        <div class="container">
            <h1>{{ .constant.siteName }} <span class="theme-badge">{{ .currentTheme }}</span></h1>
            <div class="theme-info">当前主题: 深色主题 - 护眼的夜间模式</div>
            <nav>
                <a href="/">首页</a>
                <a href="/posts">文章</a>
                <a href="/login">登录</a>
                <a href="/themes">主题管理</a>
            </nav>
        </div>
    </header>
    <main>
        <div class="container">
            {{ template "content" . }}
        </div>
    </main>
    <footer>
        <div class="container">
            <p>&copy; {{ .variable.year }} {{ .constant.siteName }} - v{{ .constant.version }}</p>
            <p>当前主题: {{ .currentTheme }} (深色模式)</p>
        </div>
    </footer>
    {{ template "script" . }}
</body>

</html>